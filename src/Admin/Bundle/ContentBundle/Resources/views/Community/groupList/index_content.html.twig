<table class="table">
    <tr>
        <td>讨论组名</td>
        <td>所属分类</td>
        <td>操作</td>
    </tr>
{% for group in groups %}
    <tr>
        <td>{{ group.title }}</td>
        <td>{{ group.category.title }}</td>
        <td><a href="{{ url('community_group_edit' , {'id' : group.id} ) }}">编辑</a></td>
    </tr>
{% endfor %}
</table>